pub enum LightType = extern UnityEngine.LightType {
  Spot = extern Spot,
  Directional = extern Directional,
  Point = extern Point,
  Area = extern Area,
  Rectangle = extern Rectangle,
  Disc = extern Disc,
}
