pub enum TransparencySortMode = extern UnityEngine.TransparencySortMode {
  Default = extern Default,
  Perspective = extern Perspective,
  Orthographic = extern Orthographic,
  CustomAxis = extern CustomAxis,
}
