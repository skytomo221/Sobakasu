pub enum WrapMode = extern UnityEngine.WrapMode {
  Default = extern Default,
  Once = extern Once,
  Clamp = extern Clamp,
  Loop = extern Loop,
  PingPong = extern PingPong,
  ClampForever = extern ClampForever,
}
