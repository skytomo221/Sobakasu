pub enum CameraClearFlags = extern UnityEngine.CameraClearFlags {
  Skybox = extern Skybox,
  Color = extern Color,
  SolidColor = extern SolidColor,
  Depth = extern Depth,
  Nothing = extern Nothing,
}
