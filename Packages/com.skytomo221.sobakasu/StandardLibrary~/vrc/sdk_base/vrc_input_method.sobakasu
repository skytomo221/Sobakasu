pub impl VRCInputMethod = extern VRC.SDKBase.VRCInputMethod {
  pub static fn equals(obj_a: object, obj_b: object) -> bool
    = extern System.Object.Equals(obj_a, obj_b)

  pub static fn reference_equals(obj_a: object, obj_b: object) -> bool
    = extern System.Object.ReferenceEquals(obj_a, obj_b)

  pub fn get_type() -> System.Type
    = extern self.GetType()

  pub static fn controller -> Self
    = extern Self.Controller

  pub static fn count -> Self
    = extern Self.Count

  pub static fn gaze -> Self
    = extern Self.Gaze

  pub static fn generic -> Self
    = extern Self.Generic

  pub static fn hp_motion_controller -> Self
    = extern Self.HPMotionController

  pub static fn index -> Self
    = extern Self.Index

  pub static fn keyboard -> Self
    = extern Self.Keyboard

  pub static fn mouse -> Self
    = extern Self.Mouse

  pub static fn oculus -> Self
    = extern Self.Oculus

  pub static fn open_xr_generic -> Self
    = extern Self.OpenXRGeneric

  pub static fn osc -> Self
    = extern Self.Osc

  pub static fn pico -> Self
    = extern Self.Pico

  pub static fn quest_hands -> Self
    = extern Self.QuestHands

  pub static fn steam_vr2 -> Self
    = extern Self.SteamVR2

  pub static fn touch -> Self
    = extern Self.Touch

  pub static fn vive_xr -> Self
    = extern Self.ViveXr

  pub static fn vive -> Self
    = extern Self.Vive
}
